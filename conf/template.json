{
    "session_name": "${PROJECT}",
    "start_directory": "${DIR}",
    "windows": [
        {
            "panes": [
                "blank",
                "blank"
            ]
        }
    ]
}
